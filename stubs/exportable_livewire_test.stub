<?php

namespace {{ namespace }};

use {{ factoryNamespace }}\{{ factoryClass }};
use {{ livewireNamespace }}\{{ livewireClass }};
use Tests\TestCase;
use Livewire\Livewire;

class {{ class }} extends TestCase
{
    public function it_will_mount_with_factory(): void
    {
        $factory = new {{ factoryClass }}();

        $component = Livewire::test({{ livewireClass }}::class, ['factory' => $factory]);

        $component->assertStatus(200);
    }

    public function it_will_render_view(): void
    {
        $factory = new {{ factoryClass }}();

        $component = Livewire::test({{ livewireClass }}::class, ['factory' => $factory]);

        $component->assertViewIs('livewire.{{ view }}');
    }
} 